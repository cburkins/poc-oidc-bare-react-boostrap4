import React from "react";

export const HomePage = () => {
    return (
        <div style={{ margin: "40px" }}>
            <div>Home Page (which is public)</div>
            <div style={{ color: "purple" }}>NOTE: The login redirect always goes here</div>
        </div>
    );
};
